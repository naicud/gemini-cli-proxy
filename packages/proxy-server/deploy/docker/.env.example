# Gemini Proxy Server - Environment Variables
# Copy to .env and fill in your values

# ============================================
# AUTHENTICATION (choose ONE method)
# ============================================

# Method 1: Gemini API Key (recommended for Docker)
GEMINI_API_KEY=

# Method 2: Vertex AI (requires GCP setup)
# GOOGLE_CLOUD_PROJECT=your-gcp-project

# Method 3: OAuth tokens (mount ~/.gemini volume)
# No env var needed, just uncomment volumes in docker-compose.yml

# Method 4: Consumer API Key (protect your proxy)
PROXY_API_KEY=

# ============================================
# SERVER CONFIGURATION
# ============================================

# Server port (default: 3000)
PORT=3000

# CORS origins - comma-separated list or * for all
CORS_ORIGINS=*

# Include thinking/reasoning in responses
INCLUDE_THINKING=true
